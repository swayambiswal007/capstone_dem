# GLOF Intelligence Console V2

Run `pip install flask` then `python app.py`.
Open http://127.0.0.1:5000.

This is the frontend shell. Terrain and prediction values are placeholders until connected to the existing DEM/DINOv2 API and temporal GLOF model.
